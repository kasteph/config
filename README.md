# config

Clone into `~/.config`: `git clone git@github.com:stephsamson/config.git ~/.config`.

## Bill of Items

* emacs
* doom
* rofi
* alacritty
* poetry
* pyenv

## Symlinks

From `$HOME`:

``` sh
$ ln -Ts ~/.config/.$.doom/ .
$ ln -s ~/.config/.gitconfig .
$ ln -s ~/.config/.bash* .
```

